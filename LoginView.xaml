<UserControl x:Class="GameOnSystem.LoginView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:GameOnSystem"
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="800"
             Style="{StaticResource BaseStyle}">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="150"></RowDefinition>
            <RowDefinition Height="Auto"></RowDefinition>
        </Grid.RowDefinitions>
        <Grid.ColumnDefinitions>
            <ColumnDefinition></ColumnDefinition>
            <ColumnDefinition Width="Auto"></ColumnDefinition>
            <ColumnDefinition></ColumnDefinition>
        </Grid.ColumnDefinitions>

        <TextBlock Grid.Row="0" Grid.Column="1" Text="Login" FontSize="30" VerticalAlignment="Bottom" HorizontalAlignment="Center" Margin="0,0,0,10"></TextBlock>

        <StackPanel Grid.Row="1" Grid.Column="1" VerticalAlignment="Bottom" HorizontalAlignment="Center" Margin="0,-2,0,0">
            <DockPanel Margin="0,0,0,5">
                <TextBlock x:Name="Email_Label" Text="Email: " FontSize="15" Width="100" Height="25"/>
                <TextBox x:Name="Email_InputField" Width="200" FontSize="14" Height="25"/>
            </DockPanel>
            <DockPanel Margin="0,0,0,10">
                <TextBlock x:Name="Password_Label" Text="Password: " FontSize="15" Width="100" Height="25"/>
                <PasswordBox x:Name="Password_InputField" Width="200" FontSize="14" Height="25"/>
            </DockPanel>
            <Button x:Name="LoginButton" Content="Login" Width="100" Height="25" Click="LoginButton_Click"/>
            <TextBlock x:Name="ResultInfo"></TextBlock>
        </StackPanel>
    </Grid>
</UserControl>
