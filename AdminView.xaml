<UserControl x:Class="GameOnSystem.AdminView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:GameOnSystem"
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="800"
             Style="{StaticResource BaseStyle}">
    <Grid>

        <Grid.RowDefinitions>
            <RowDefinition Height="48"></RowDefinition>
            <RowDefinition Height="2"></RowDefinition>
            <RowDefinition Height="*"></RowDefinition>
        </Grid.RowDefinitions>

        <Grid Grid.Row="0">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*"></ColumnDefinition>
                <ColumnDefinition Width="120"></ColumnDefinition>
            </Grid.ColumnDefinitions>

            <TextBlock Name="TitleTextBlock" Grid.Column="0" Text="GameOn - Admin" FontSize="30" Margin="10,0,0,0"></TextBlock>

            <Button Name="ChangeToUserView" Grid.Column="1" Content="Show UserView" Height="25" Width="100" Click="ChangeToUserView_Click"/>

        </Grid>

        <Border Grid.Row="1" Background="#606060"></Border>

        <Grid Grid.Row="2">
            <ScrollViewer Padding="10">
                <StackPanel Orientation="Vertical">
                    <Border Style="{StaticResource AdminViewSegmentWrapper}">
                        <Grid>
                            <Grid.RowDefinitions>
                                <RowDefinition Height="30"></RowDefinition>
                                <RowDefinition Height="2"></RowDefinition>
                                <RowDefinition Height="*"></RowDefinition>
                            </Grid.RowDefinitions>
                            <TextBlock Grid.Row="0" Text="Utgåvor" FontSize="20" Padding="0,0,0,5"></TextBlock>
                            <Border Grid.Row="1" Background="#454545" CornerRadius="2"></Border>
                            <ContentControl Grid.Row="2" x:Name="AdminSegment_Edition"></ContentControl>
                        </Grid>
                    </Border>

                    <Border Style="{StaticResource AdminViewSegmentWrapper}">
                        <Grid>
                            <Grid.RowDefinitions>
                                <RowDefinition Height="30"></RowDefinition>
                                <RowDefinition Height="2"></RowDefinition>
                                <RowDefinition Height="*"></RowDefinition>
                            </Grid.RowDefinitions>
                            <TextBlock Grid.Row="0" Text="Kategorier" FontSize="20" Padding="0,0,0,5"></TextBlock>
                            <Border Grid.Row="1" Background="#454545" CornerRadius="2"></Border>
                            <ContentControl Grid.Row="2" Name="AdminSegment_Category"></ContentControl>
                        </Grid>
                    </Border>

                    <Border Style="{StaticResource AdminViewSegmentWrapper}">
                        <Grid>
                            <Grid.RowDefinitions>
                                <RowDefinition Height="30"></RowDefinition>
                                <RowDefinition Height="2"></RowDefinition>
                                <RowDefinition Height="*"></RowDefinition>
                            </Grid.RowDefinitions>
                            <TextBlock Grid.Row="0" Text="Användare" FontSize="20" Padding="0,0,0,5"></TextBlock>
                            <Border Grid.Row="1" Background="#454545" CornerRadius="2"></Border>
                            <ContentControl Grid.Row="2" Name="AdminSegment_Users"></ContentControl>
                        </Grid>
                    </Border>

                    <Border Style="{StaticResource AdminViewSegmentWrapper}">
                        <Grid>
                            <Grid.RowDefinitions>
                                <RowDefinition Height="30"></RowDefinition>
                                <RowDefinition Height="2"></RowDefinition>
                                <RowDefinition Height="*"></RowDefinition>
                            </Grid.RowDefinitions>
                            <TextBlock Grid.Row="0" Text="Grupper" FontSize="20" Padding="0,0,0,5"></TextBlock>
                            <Border Grid.Row="1" Background="#454545" CornerRadius="2"></Border>
                            <ContentControl Grid.Row="2" Name="AdminSegment_Groups"></ContentControl>
                        </Grid>
                    </Border>
                </StackPanel>
            </ScrollViewer>
        </Grid>
    </Grid>
</UserControl>
